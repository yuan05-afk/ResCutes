/**
 * Database seed script — run with npm run db:seed when DATABASE_URL is configured.
 * Demo data is also available in-memory via src/lib/data/demo-store.ts.
 */
console.log(
  "ResCutes seed: Demo data is loaded in-memory. Configure DATABASE_URL and run drizzle migrations to persist to Neon.",
);
